# Correcciones 2026-08-23

- Corregido `/create_product`: eliminada referencia a `product.id` antes de construir el producto.
- Restaurado flujo de imágenes manuales: el catálogo/POS solo usa `image_path` local.
- Eliminados `image_url` e `image_file` de la plantilla de importación/exportación de productos.
- Las importaciones CSV/XLSX conservan las fotos manuales existentes y no las reemplazan.
- El formulario limita nombre a 150 caracteres y SKU a 50, igual que PostgreSQL.
- Productos LOT/SERIAL no pueden iniciar con stock directo; deben registrar trazabilidad desde la ficha.
- El stock inicial de productos nuevos e importados registra movimiento de entrada en Kardex.
- se migraron las vistas legacy al shell común duplicadas detectadas por el auditor estático.
- `scripts/static_release_audit.py` pasa correctamente.
- Tests de regresión de producto: 4/4 pasan.

La columna legacy `products.image_url` se conserva únicamente por compatibilidad con la revisión Alembic actual `d7a1c4e9b206`; la aplicación ya no la usa para mostrar productos.
